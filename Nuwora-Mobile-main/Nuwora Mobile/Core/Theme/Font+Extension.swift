import SwiftUI

extension Font {
    static let nHeading = Font.system(.largeTitle, design: .rounded, weight: .bold)
    static let nTitle = Font.system(.title2, design: .rounded, weight: .semibold)
    static let nBody = Font.body
    static let nCaption = Font.caption
    static let nNumber = Font.system(.title, design: .rounded, weight: .bold)
}
